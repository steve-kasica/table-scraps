- wrangling actions:
    - amend:
        - detrend:
            - adjust for inflation
            - adjust for season
            - compute index number    
        - encode table-level data:
            - encode table identification in row
            - encode table summary data in row  # closely related to peel & merge            
        - formulate performance metric:
            - calculate standardized score:
                - calculate z-score
            - figuring a normalized rate:
                - calculate percentage
                - calculate per 1k
                - calculate proportion
            - calculate central tendency:
                - calculate mean
            - quantify change:
                - calculate percentage difference
                - calculate difference
            - calculate spread  # TODO what is this?            
        - key generation:
            - create a semi-unique key  # e.g. concat city state for group or join
            - create a unique key
            - concatenate columns  # TODO rename concatenate columns into key (has this always been the case            
        - rank entities:
            - assign ranks  # TODO explicitly compute ranks in a sorted table
            - break ties
            - sort table            
            - sort table and view top
    - clean:
        - trim fat:
            - reduce dimensionality:
                - drop columns
                - select columns
            - prune data:
                - trim by date range
                - trim by geographic area
                - trim by quantitative threshold
                - filter rows
                - drop erroneous rows
                - remove incomplete data
                - set data confidence threshold
                - deduplicate:
                    - prevent double-counting
                    - drop entirely duplicate rows
                    - drop rows with duplicate value in one column
                    - remove all rows but the master record  # uh what?                
        - edit:
            - handle incomplete data:
                - replace NA values
            - fix data errors manually
            - fix incorrect calculation
            - regular expression replace  # TODO re-examine this code, can we be more specific as to why?
            - resolve entity names:
                - manually perform name entity resolution  # TODO renamed from resolve entity names
                - strip whitespace  # TODO rename all to trim whitespace
            - scale values:
                - Log-ify values
                - scale numeric values  # TODO linear function                        
        - format:
            - value formatting:
                - change case
                - change date format
                - round floating point
            - meta formatting:
                - canonicalize column names
                - change column data type
        - separate:
            - extract property from datetime
            - slice column values
            - split column on delimiter
            - get unique values            
    - integrate:
        - consolidate:
            - union tables
        - supplement:
            - right join tables
            - left join tables
            - join with lookup table  # similar to ammend
            - merge metadata
            - ping web service:
                - add calculated column from axillary data
                - geocode addresses            
        - intersect:
            - inner join tables
            - natural join  # TODO what does this really mean?
        - other:
            - cartesian product  # seems like the opposite of intersection
            - self join table  # Is this just fancy duplication?          
    - transform:
        - summarize:
            - aggregate:
                - group by single column
                - group by multiple columns
                - create rolling window  # TODO explain rolling window                    
                - construct pivot table
                - create a crosstab            
            - calculate:
                - sum column values
                - get max value
                - count value frequency
                - count unique values in column
        - reshape:
            - spread table
            - gather table

- wrangling support:  # maybe wrangling environment
    - display dataset:
        - check sanity:
            - check results of previous operation
            - peek at data    
            - inspect table schema            
        - display a table:
            - format table display
            - display entire table
        - Understand distribution:
            - plot histogram
            - plot stacked bar chart
            - plot stacked column chart
            - plot scatterplot
            - plot trendline
            - plot column chart
            - plot violin plot
            - plot boxplot
            - plot scree plot
    - documentation:
        - annotate workflow
    - export data:
        - cache results from external service    
        - export intermediate results
        - export results            
    - workflow building:
        - think computationally:
            - architect a subroutine
            - architect repeating process
        - toggle step on and off
    - data acquisition:
        - extracted data:
            - pull tables out of pdf
            - scrape web for data

        - create:
            - construct table manually
            - generate data computationally
           # - collect raw data # First-hand observations or logs (Have not coded that article yet)
    - data properties:
        - quality:
            - use previously cleaned data    
        - structure:
            - use structured ascii
            - use geospatial data
            - use tabular data
        - source:
            - use public disclosure data
            - use public data
            - use academic data
            - use non-public, provided data
            - use another news orgs data
            - use data from colleague        
- wrangling purpose:
    - analysis:
        - extract single value        
        - analyze principle components
        - fit a generalized linear model
        - look for trends
        - find most frequently occurring  # closely related to sort
        - find worst offender
        - count number of records
    - wrangle data for graphics
    - combine seemingly disparate datasets
- wrangling strategies:
    - value replacement
    - Preserve existing values
    - table splitting:
        - split, compute, and merge
        - split and compute
        - peel and merge
    - tolerate dirty data
    - omits data quality exploration
    - temporary joining column

- wrangling pain points:
    - repetitive code
    - resort after merge
    - data loss from aggregation
    - encoding provenance in data