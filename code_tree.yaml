- transform:
    - combine:
        - inner join tables
        - cartesian product
        - append:
            - right join tables
            - left join tables
            - append to table
            - merge data sources
            - merge metadata
        - consolidate data sources:
            - union tables
    - reshape:
        - melt table
        - create a lookup table  # rename to use lookup table
        - drop columns
        - add calculated column from axillary data
#        - add column from intra-table calculation  # Add this to some documents
        - changing resolution:
            - aggregate and calculate:
                - group by
                - multi-dimensional transform
                - create rolling window
                - sum column
                - construct pivot table
    - split:
        - split and merge  # split, compute, and merge
        - architect parallel workflows        
- prep for analysis:
    - calculate frequency:
        - count column values
        - count rows
        - count unique values in column
    - rank entities by common metric:
        - formulate performance metric:
            - figure a rate:
                - calculate percentage
                - calculate per 1k
                - calculate proportion
            - calculate central tendency:
                - calculate mean
            - compare trends over time:
                - calculate percentage difference
                - calculate difference
            - calculate spread
            - calculate standardized scores:
                - calculate z-score
        - sort table and view top        
- cleaning:
    - detrending:
        - adjust for inflation
        - adjust for season
        - compute index number    
    - deduplicate:
        - prevent double-counting
        - drop entirely duplicate rows
        - drop rows with duplicate value in one column
        - create a unique key
    - format column values:
        - strip whitespace
        - change case
        - resolve entity names        
    - extract value from column:
        - regular expression replace    
        - extract property from datetime
        - slice column values
    - pruning:
        - filter rows
        - drop erroneous rows
        - remove incomplete data
    - schema:
        - canonicalize column names
        - change column data type
    - fix data errors manually
    - sort table    
- document provenance:
    - encode table name as new column
- build workflow:
    - document:
        - annotate workflow        
    - think computationally:
        - architect a subroutine
        - architect repeating process
        - redundant computation  # rename to repetitive code
    - export data:
        - export intermediate results
        - export results
        - export data for graphics
- acquire:
    - data type:
        - use non-tabular data:
            - use structured ascii
            - import structured ASCII
            - use geospatial data
        - use tabular data
    - data source:
        - extraction:
            - pull tables out of pdf
            - scrape web for data
        - use existing data:
            - use third-party data
            - use another news orgs data
        - create new data:
            - construct table manually
            - collect raw data # First-hand observations or logs 
            - generate data computationally # for what Heat and Humidity did
- display:
    - display a table:
        - peek at data
        - inspect table schema        
        - format table display
    - Understand data:
        - plot histogram
        - plot stacked bar chart
        - plot stacked column chart
        - plot scatterplot        